#include<stdio.h>

void InsertSort(int *a,int n){
    int min = 1000000;
    for (int i = 0; i < n; i++)
    {
        for (int j = i-1; j > 0; j--)
        {
            if (a[i]<a[j])
            {
                
            }
        }
    }
}